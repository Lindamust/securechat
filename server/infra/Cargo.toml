[package]
name = "infra"
version = "0.0.0"
edition = "2024"

[dependencies]
sqlx.workspace = true
uuid.workspace = true
chrono.workspace = true
tokio.workspace = true

pipeline = { path = "../pipeline" }
